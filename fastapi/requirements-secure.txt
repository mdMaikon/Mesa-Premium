# FastAPI Core - Updated to secure versions
fastapi>=0.115.6  # Fixed CVE-2024-24762 (ReDoS vulnerability) - requires 0.109.1+
uvicorn[standard]>=0.34.0  # Latest stable version with security updates
pydantic>=2.10.4  # Latest stable version
slowapi==0.1.9  # No known vulnerabilities

# Database
mysql-connector-python==9.3.0  # Latest stable version
python-dotenv==1.1.0  # No known vulnerabilities

# Web Automation
selenium==4.33.0  # Latest stable version

# Background Tasks
celery==5.4.0  # No known vulnerabilities
redis==5.2.1  # No known vulnerabilities

# Utilities
pillow==11.2.1  # Latest stable version
pandas==2.3.0  # No known vulnerabilities

# HTTP and Security - CRITICAL UPDATES
requests>=2.32.4  # Fixed CVE-2024-35195, CVE-2024-47081 (cert verification, .netrc leak)
httpx>=0.28.1  # Latest stable version
urllib3>=2.5.0  # Fixed CVE-2025-50182, CVE-2025-50181 (redirect control issues)
cryptography>=44.0.0  # Fixed multiple critical OpenSSL CVEs (42.0.4+ required)
starlette>=0.41.3  # Fixed CVE-2024-47874 (DoS vulnerability) - requires 0.40.0+
jinja2>=3.1.6  # Fixed multiple XSS and sandboxing CVEs
idna>=3.10  # Fixed CVE-2024-3651 (DoS via crafted input)
setuptools>=78.2.0  # Fixed CVE-2025-47273 (Path Traversal/RCE) - requires 78.1.1+
twisted>=24.7.0  # Fixed CVE-2024-41810, CVE-2024-41671 (XSS, request ordering)

# Remove vulnerable dependencies
# configobj - vulnerable to CVE-2023-26112 (ReDoS) - removed if not essential

# Development and Testing - Updated versions
pytest>=8.4.1  # Latest stable version
pytest-asyncio>=0.25.0  # Latest stable version
pytest-mock==3.12.0  # No known vulnerabilities
pytest-timeout==2.1.0  # No known vulnerabilities
pytest-cov>=6.0.0  # Latest stable version
factory-boy==3.3.0  # No known vulnerabilities

# Security Tools - MANDATORY for continuous monitoring
pip-audit>=2.9.0  # For continuous vulnerability scanning
safety>=3.5.2  # Alternative security scanner

# Production Security (optional but recommended)
gunicorn>=23.0.0  # Production WSGI server with latest security patches
sentry-sdk>=2.20.0  # Error monitoring and security incident tracking